# Copy this to .env file and fill in your credentials

# AWS Transcribe Configuration
# Get these from AWS Console -> IAM -> Users -> Security Credentials
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1

# Optional: Transcribe Settings
AWS_TRANSCRIBE_LANGUAGE=en-US
AWS_TRANSCRIBE_SAMPLE_RATE=16000
AWS_TRANSCRIBE_MAX_SPEAKERS=4

# Twilio Configuration (existing)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# ngrok Configuration (existing)
NGROK_DOMAIN=your_ngrok_domain
NGROK_AUTH_TOKEN=your_ngrok_auth_token 